<center>
    <h1 class="loggedInHeading">Videobog</h1>
    <p class="loggedInText">Du kan optage video og tilføje til din personlige videobog. Klik
        efterfølgende på en video for at tilføje tekst.</p>
</center>

<div id="tilføjBillede">
    <p>Tilføj video til videobogen</p>
</div>

<div class="container">
    <div class="row">
        <div class="col-md-6 m-auto">
            <form action="/videobog/videoupload" method="POST" enctype="multipart/form-data">
                <div class="custom-file mb-3">
                    <p>Optag video: <input type="file" accept="image/*" id="capture" capture="camera" name="file" class="custom-file-input">
                
                    <label for="file" class="custom-file-label">Vælg fil</label>
                </div>
                <input type="submit" value="Tilføj" class="btn btn-primary btn-block">
            </form>
            <hr>
            {{#if files}}
            {{#each files}}
            <div class="card card-body mb-3">
                <video width="320" height="240" controls>
                         <source src="videobog/image/{{filename}}" type="video/mp4">Din browser understøtter ikke denne filtype</video>
                <form method="POST" action="/videobog/files/{{_id}}?_method=POST">
                <input type="text" name="billedeTextFelt" class="btn btn-danger btn-block mt-4"></input>
                    {{text}}
                    </form>
                <form method="POST" action="/videobog/files/{{_id}}?_method=DELETE">
                    <button class="btn btn-danger btn-block mt-4">Slet</button>
                </form>
            </div>
            {{/each}}
            {{/if}}
        </div>
    </div>
</div>